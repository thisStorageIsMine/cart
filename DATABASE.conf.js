const HOST = "127.0.0.1", // Localhost
      USER = "root",
      PASSWORD = "",
      DATABASE = "ajax";



const dbSettings = {
    HOST,
    USER,
    PASSWORD,
    DATABASE
}

export default dbSettings;